import Link from "next/link";
import { notFound } from "next/navigation";
import { createAdminClient } from "@/lib/supabase/admin";
import { createClient } from "@/lib/supabase/server";
import { orderStatus } from "@/lib/order-status";
import OrderChat from "@/app/pedidos/[id]/OrderChat";
import { updateOrderChat, updateOrderStatus } from "../actions";

export default async function AdminOrderPage({params}:{params:Promise<{id:string}>}){
  const {id}=await params; const admin=createAdminClient(); const {data:order}=await admin.from("orders").select("*,order_items(product_name,unit_price,quantity),profiles(nickname)").eq("id",id).maybeSingle(); if(!order)notFound();
  const {data:{user}}=await (await createClient()).auth.getUser(); if(!user)notFound();
  const {data:messages}=await admin.from("order_messages").select("id,user_id,message,created_at,profiles(nickname,avatar_url)").eq("order_id",id).order("created_at");
  let proofUrl:string|null=null; if(order.proof_path){const {data}=await admin.storage.from("payment-proofs").createSignedUrl(order.proof_path,600);proofUrl=data?.signedUrl??null;}
  const status=orderStatus[order.status]??{label:order.status,className:""}; const chatAvailable=["paid","preparing_delivery","delivered"].includes(order.status); const canSend=chatAvailable&&!order.chat_closed_at;
  const buttons:string[][]=order.status==="proof_submitted"||order.status==="under_review"
    ? [["paid","Confirmar pagamento"],["proof_rejected","Recusar comprovante"],["cancelled","Cancelar pedido"]]
    : order.status==="paid"||order.status==="preparing_delivery"
      ? [["delivered","Marcar como entregue"],["cancelled","Cancelar pedido"]]
      : order.status==="awaiting_payment"||order.status==="proof_rejected"
        ? [["cancelled","Cancelar pedido"]]
        : [];
  return <main className="min-h-screen bg-[#080812] p-6 text-white"><div className="mx-auto max-w-4xl"><Link href="/admin/pedidos" className="text-sm font-bold text-zinc-400">← Todos os pedidos</Link><div className="mt-6 flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm text-violet-400">Pedido</p><h1 className="text-3xl font-black">{order.order_code}</h1><p className="mt-2 text-zinc-500">Cliente: {order.profiles?.nickname??"Cliente"} • Nick: {order.game_nickname}</p>{order.delivery_due_at&&!["delivered","cancelled"].includes(order.status)&&<p className="mt-1 text-sm text-amber-300">Prazo de entrega: {new Date(order.delivery_due_at).toLocaleString("pt-BR")}</p>}</div><span className={`rounded-full px-4 py-2 text-sm font-bold ${status.className}`}>{status.label}</span></div><section className="mt-7 rounded-2xl border border-white/10 bg-[#111122] p-6"><div className="space-y-3">{order.order_items?.map((item:{product_name:string;unit_price:number;quantity:number},index:number)=><div key={index} className="flex justify-between"><span>{item.quantity}× {item.product_name}</span><strong>{(Number(item.unit_price)*item.quantity).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</strong></div>)}</div><div className="my-5 border-t border-white/10"/><div className="flex justify-between text-xl"><strong>Total</strong><strong>{Number(order.total).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</strong></div>{proofUrl&&<a href={proofUrl} target="_blank" rel="noreferrer" className="mt-5 inline-block rounded-xl bg-sky-600 px-5 py-3 font-bold">Abrir comprovante</a>}</section>{buttons.length>0&&<section className="mt-6 rounded-2xl border border-white/10 bg-[#111122] p-6"><h2 className="font-black">Atualizar pedido</h2><div className="mt-4 flex flex-wrap gap-2">{buttons.filter(([value])=>value!=="proof_rejected").map(([value,label])=><form key={value} action={updateOrderStatus}><input type="hidden" name="order_id" value={id}/><input type="hidden" name="status" value={value}/><button className={`rounded-xl px-4 py-2 text-sm font-bold ${value==="paid"||value==="delivered"?"bg-emerald-600":"bg-red-600"}`}>{label}</button></form>)}</div>{buttons.some(([value])=>value==="proof_rejected")&&<form action={updateOrderStatus} className="mt-5 rounded-xl border border-red-500/20 bg-red-500/5 p-4"><input type="hidden" name="order_id" value={id}/><input type="hidden" name="status" value="proof_rejected"/><label htmlFor="rejection_reason" className="text-sm font-bold text-red-200">Motivo da recusa</label><textarea id="rejection_reason" name="rejection_reason" required minLength={5} maxLength={300} placeholder="Ex.: imagem ilegível ou valor diferente do pedido." className="mt-2 min-h-24 w-full rounded-xl border border-white/10 bg-[#080812] p-3 text-sm outline-none focus:border-red-400"/><button className="mt-3 rounded-xl bg-red-600 px-4 py-2 text-sm font-bold">Recusar comprovante</button></form>}</section>}{chatAvailable&&<><section className="mt-6 flex flex-wrap items-center justify-between gap-4 rounded-2xl border border-white/10 bg-[#111122] p-5"><div><h2 className="font-black">Atendimento do pedido</h2><p className="mt-1 text-sm text-zinc-500">{canSend?"Chat aberto para a equipe e o cliente.":"Chat encerrado. O histórico continua visível."}</p></div><form action={updateOrderChat}><input type="hidden" name="order_id" value={id}/><input type="hidden" name="chat_action" value={canSend?"close":"open"}/><button className={`rounded-xl px-4 py-2 text-sm font-bold ${canSend?"bg-zinc-700":"bg-violet-600"}`}>{canSend?"Encerrar atendimento":"Reabrir atendimento"}</button></form></section><OrderChat orderId={id} userId={user.id} initialMessages={(messages??[]) as never[]} canSend={canSend}/></>}</div></main>;
}
