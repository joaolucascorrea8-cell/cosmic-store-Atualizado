"use client";
import { useState } from "react";
const slugify = (text: string) => text.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "").slice(0, 100);
export default function ProductBasics() {
  const [name, setName] = useState("");const [slug, setSlug] = useState("");const [custom, setCustom] = useState(false);const [unlimited, setUnlimited] = useState(true);
  return <><div className="grid gap-4 sm:grid-cols-2"><div><label htmlFor="product-name" className="admin-label">Nome do produto</label><input id="product-name" name="name" value={name} onChange={e => {setName(e.target.value);if (!custom) setSlug(slugify(e.target.value));}} className="admin-input" minLength={2} maxLength={100} placeholder="Ex.: Dragon permanente" required /></div><div><label htmlFor="product-slug" className="admin-label">Identificador (URL)</label><input id="product-slug" name="slug" value={slug} onChange={e => {setCustom(true);setSlug(slugify(e.target.value));}} className="admin-input" minLength={2} maxLength={100} pattern="[a-z0-9]+(-[a-z0-9]+)*" placeholder="dragon-permanente" required /><p className="mt-1 text-xs text-zinc-500">Gerado automaticamente.</p></div></div><div className="grid gap-4 sm:grid-cols-3"><div><label className="admin-label" htmlFor="product-price">Preço em reais (R$)</label><input className="admin-input" id="product-price" name="price" type="number" min="0" max="9999999999.99" step="0.01" placeholder="145.00" required /></div><div><label className="admin-label" htmlFor="product-stock-kind">Tipo de estoque</label><select className="admin-input" id="product-stock-kind" name="unlimited_stock" value={String(unlimited)} onChange={e => setUnlimited(e.target.value === "true")}><option value="true">Ilimitado</option><option value="false">Por quantidade</option></select></div><div><label className="admin-label" htmlFor="product-stock">Unidades no estoque</label><input className="admin-input disabled:opacity-40" id="product-stock" name="stock" type="number" min="0" max="2147483647" step="1" defaultValue="0" disabled={unlimited} required={!unlimited} /></div></div></>;
}
