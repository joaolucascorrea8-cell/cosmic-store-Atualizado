import Link from "next/link";import {notFound} from "next/navigation";import {createAdminClient} from "@/lib/supabase/admin";import {requireAdmin} from "@/lib/require-admin";import {withSignedChatAttachments} from "@/lib/chat-attachments";import SupportChat from "@/app/suporte/[id]/SupportChat";import {updateTicketStatus} from "../actions";
export const dynamic="force-dynamic";
export default async function AdminTicket({params}:{params:Promise<{id:string}>}){const {id}=await params;const user=await requireAdmin();const admin=createAdminClient();const [{data:ticket},{data:messages}]=await Promise.all([admin.from("support_tickets").select("id,subject,category,status,created_at,profiles(nickname)").eq("id",id).maybeSingle(),admin.from("support_messages").select("id,user_id,message,created_at,attachment_path,attachment_name,attachment_type,profiles(nickname)").eq("ticket_id",id).order("created_at")]);if(!ticket)notFound();const signedMessages=await withSignedChatAttachments(messages??[]);const profile=Array.isArray(ticket.profiles)?ticket.profiles[0]:ticket.profiles;return <main className="min-h-screen p-6 text-white"><div className="mx-auto max-w-4xl"><Link href="/admin/suporte" className="text-sm font-bold text-zinc-500">← Atendimentos</Link><div className="my-7 flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Suporte • {profile?.nickname??"Cliente"}</p><h1 className="mt-2 text-3xl font-black">{ticket.subject}</h1></div><form action={updateTicketStatus}><input type="hidden" name="id" value={ticket.id}/><input type="hidden" name="status" value={ticket.status==="closed"?"open":"closed"}/><button className={`rounded-xl px-4 py-2 text-sm font-bold ${ticket.status==="closed"?"bg-emerald-600":"bg-red-600"}`}>{ticket.status==="closed"?"Reabrir atendimento":"Encerrar atendimento"}</button></form></div><SupportChat ticketId={ticket.id} userId={user.id} initialMessages={signedMessages as never[]} canSend={ticket.status!=="closed"}/></div></main>}
