import Image from "next/image";
import Link from "next/link";

type CatalogCardProps = { href: string; name: string; imageUrl?: string | null; eyebrow: string; description: string };
export default function CatalogCard({ href, name, imageUrl, eyebrow, description }: CatalogCardProps) {
  return <Link href={href} className="game-tile group relative isolate block min-h-56 overflow-hidden rounded-2xl border border-white/10 bg-[#181222] sm:min-h-64"><div className="absolute inset-0 bg-[radial-gradient(circle_at_75%_25%,rgba(146,78,255,.3),transparent_65%)]" />{imageUrl ? <Image src={imageUrl} alt="" fill sizes="(max-width: 640px) 100vw, (max-width: 1000px) 50vw, 33vw" className="object-cover opacity-75 transition duration-500 group-hover:scale-105 group-hover:opacity-90" /> : <span className="absolute right-[15%] top-9 text-[7rem] font-black leading-none text-violet-400/20">{name.slice(0, 1).toUpperCase()}</span>}<div className="absolute inset-0 bg-gradient-to-t from-[#090811] via-[#090811]/40 to-transparent" /><div className="absolute inset-x-0 bottom-0 p-5"><p className="text-[10px] font-extrabold uppercase tracking-[.18em] text-violet-200">{eyebrow}</p><h3 className="mt-1 text-2xl font-black tracking-tight">{name}</h3><p className="mt-1 text-xs text-zinc-300">{description}</p><span className="mt-4 inline-flex items-center gap-2 text-xs font-black text-white">Abrir catálogo <span aria-hidden="true">↗</span></span></div></Link>;
}
