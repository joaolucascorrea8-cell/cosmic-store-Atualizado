type Feedback={id:string;rating:number;comment:string;author_nickname:string;created_at:string};

export default function StoreFeedbacks({feedbacks}:{feedbacks:Feedback[]}){if(!feedbacks.length)return null;return <section className="shell"><p className="eyebrow">Compras verificadas</p><h2 className="section-title">Quem compra, recomenda</h2><div className="mt-8 grid gap-4 md:grid-cols-3">{feedbacks.slice(0,3).map(item=><article key={item.id} className="surface card-hover rounded-3xl p-6"><div className="text-amber-300" aria-label={`${item.rating} estrelas`}>{"★".repeat(item.rating)}<span className="text-zinc-700">{"★".repeat(5-item.rating)}</span></div><p className="mt-5 min-h-14 leading-7 text-zinc-300">“{item.comment}”</p><div className="mt-6 flex items-center justify-between gap-3 border-t border-white/[.06] pt-4 text-xs"><strong>{item.author_nickname}</strong><span className="text-emerald-400">✓ Compra verificada</span></div></article>)}</div></section>}
